# F4.2 — Implementación

Fecha: 2026-09-13  
Rama: `feature/ventas-automaticas`  
Auditoría: `docs/F4.2_AUDITORIA_INICIAL.md`

## Qué se hizo

### Fase 1 — Dominio

- `src/lib/commerce-policy.ts`: `resolveAvailability`, `isPurchasable`, `isMerchantEligible`, buckets, `merchantAvailability`
- `getAccionComercial` ahora deriva de `isPurchasable`
- Quickview catálogo (`catalogo-cliente.ts`) alineado a la misma política (antes filtraba por `tipo_comercial=consumible`)
- `crear-pago` valida con la misma política
- Tests: `commerce-policy.test.ts`, `comercial.test.ts`

### Fase 2–3 — Inventario / webhooks

- Migración `supabase/migrations/20260913140000_f42_stock_reservas_auditoria.sql`
  - `stock_reservas` + RPCs `reservar_stock`, `liberar_*`, `consumir_*`, `stock_reservado_activo`
  - `auditoria_catalogo` (insert con `actor_id = auth.uid()`)
- `crear-pago`: reserva tras crear pedido; libera si falla gateway o stock
- `registrarPedidoPagado`: consume reservas
- `notificarEstadoPedido`: libera en estados terminales no pagados
- Tests concurrencia conceptuales: `stock-availability.test.ts`

### Fase 4 — Admin

- Vista `#/compra-directa` (roles ventas/catalogo/owner/admin)
- Edición precio_regular / stock / disponible / activo + intento de auditoría

### Fase 5 — Claims

- Sin claims “envío 24h” / “recibe mañana” nuevos (no hay motor logístico)
- CTA sigue “agregar / consultar / cotizar” según política

### Fase 6 — Merchant

- Fuente canónica única: Edge Function `generar-feed-google`
- Serialización compartida: `src/lib/merchant-feed.ts` (+ tests XML)
- **No** se creó `feed-google.xml.ts` Astro (site `output: static`)

### Fase 8 (parcial) — Abandono

- `recordatorio-carritos` omite envío si `clientes.consentimiento_datos !== true`

### No hecho / bloqueado

- Bundles kits (sin componentes/margen humanos)
- Seed `COMPRAHOY` 10% (sin política de margen)
- Columna `requiere_invima`
- Mapping `google_product_category`
- Alta/verificación Google Merchant Center (humano)
- Rewrite Hostinger `/feed-google.xml` → function URL (ops)
- Aplicar migración en BD producción (ops)
- Prueba de concurrencia real en Postgres (requiere migración aplicada)

## Archivos clave

```text
docs/F4.2_AUDITORIA_INICIAL.md
docs/F4.2_IMPLEMENTACION.md
docs/F4.2_RUNBOOK.md
VALIDACION.md
src/lib/commerce-policy.ts
src/lib/stock-availability.ts
src/lib/merchant-feed.ts
src/lib/comercial.ts
src/admin/admin-app.ts
supabase/migrations/20260913140000_f42_stock_reservas_auditoria.sql
supabase/functions/_shared/stock-reservas.ts
supabase/functions/_shared/post-pago.ts
supabase/functions/crear-pago/index.ts
supabase/functions/generar-feed-google/index.ts
supabase/functions/recordatorio-carritos/index.ts
```
