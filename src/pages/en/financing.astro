---
/**
 * Financing EN — stub with COPY_CLIENTE_REVISAR
 */
import Layout from '../../layouts/Layout.astro'
import { buildPageTitle, buildCanonical } from '../../lib/seo'

const locale = 'en'
const title = buildPageTitle('Financing — Medical Equipment Acquisition Plans')
const description =
  'COPY_CLIENTE_REVISAR — Flexible acquisition plans for biomedical equipment for healthcare institutions.'
const canonical = buildCanonical('/en/financing')
---

<Layout
  title={title}
  description={description}
  canonical={canonical}
  locale={locale}
  currentPath="/en/financing"
>
  <div style="padding-top:130px;padding-bottom:var(--space-2xl);background:var(--off)">
    <div class="container">
      <h1>COPY_CLIENTE_REVISAR — Financing</h1>
      <p style="color:var(--text-mid)">
        English financing content — pending client review. BLOQUEANTE_LEGAL.
      </p>
    </div>
  </div>
  <section class="section container">
    <a href="/en/contact" class="btn btn-primary">Contact an Advisor</a>
  </section>
</Layout>
